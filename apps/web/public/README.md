# Public Pages configuration

`_headers` and `_redirects` are copied into the Cloudflare Pages artifact. Advertising markup is injected only into public content and only when a valid publisher ID is explicitly configured.
