name: Legacy Android TWA build (disabled)

# Retained for history. Production Android builds use the Pages v3 workflow only.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Legacy workflow intentionally disabled
    runs-on: ubuntu-24.04
    steps:
      - run: echo "LEGACY_ANDROID_WORKFLOW_DISABLED=PASS"
