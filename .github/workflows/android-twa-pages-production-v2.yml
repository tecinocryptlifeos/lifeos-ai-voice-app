name: Legacy Android Pages build v2 (disabled)

# Retained for history. Production Android builds use v3 only.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Legacy v2 workflow intentionally disabled
    runs-on: ubuntu-24.04
    steps:
      - run: echo "LEGACY_ANDROID_V2_WORKFLOW_DISABLED=PASS"
